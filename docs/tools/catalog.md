# Tool catalog

_This file is generated by `npm run docs:tools` from the live `defineTool` registry. Do not edit by hand._

Total tools registered: **90** (**155** operations).

## Tools by permission group

### `utility` (7 tools, 7 operations)

### `gojira.bindApiToken`

Bind a per-user Atlassian API token for tools that don't accept OAuth (JSM admin, automation, Confluence). The site_url's real cloudId is resolved from its tenant_info endpoint and must match this instance's pinned cloudId; the credential is validated via /rest/api/3/myself and stored encrypted at rest.

- **Group:** `utility`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** no
- **Input:**
  - `email`: string — The Atlassian account email used as Basic-auth username.
  - `token`: string — An Atlassian API token generated at id.atlassian.com.
  - `site_url`: string — Bare site host, e.g. 'acme.atlassian.net' — used as REST base.
  - `cloud_id`: string

### `gojira.getOperation`

Retrieve a single journal entry by opId, including before/after snapshots.

- **Group:** `utility`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** no
- **Input:**
  - `op_id`: string

### `gojira.health`

Returns server liveness, Redis ping, and OAuth issuer URL.

- **Group:** `utility`
- **Auth method:** none
- **Destructive:** no
- **Requires cloudId:** no
- **Input:** _(no parameters)_

### `gojira.listEnabledTools`

Lists the tools available to this caller, given the deployment's operator allowlist and org-admin gate. Pass available_only:true to omit tools this instance does not serve.

- **Group:** `utility`
- **Auth method:** none
- **Destructive:** no
- **Requires cloudId:** no
- **Input:**
  - `available_only`: boolean

### `gojira.listRecentOperations`

List the caller's recent operations from the journal. 30-day rolling window by default.

- **Group:** `utility`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** no
- **Input:**
  - `limit`: number (default: 25)
  - `since`: string
  - `until`: string

### `gojira.revertOperation`

Reverts a previously-journaled operation (where mechanically possible). Logs the revert itself as a new journal entry. Use `commit: true` to apply.

- **Group:** `utility`
- **Auth method:** oauth_or_api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `op_id`: string
  - `commit`: boolean

### `gojira.whoami`

Returns the caller's identity, accessible cloudIds, pinned cloudId, deployment surface info, and bound-API-token presence.

- **Group:** `utility`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** no
- **Input:** _(no parameters)_

---

### `read_jsm_admin` (17 tools, 17 operations)

### `forms.getFormTemplate`

Get a project form template by id, including its full `design` document (use this to export/adapt).

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `projectIdOrKey`: string
  - `formId`: string

### `forms.getIssueFormAnswers`

Get a submitted issue form's answers in simplified format.

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `issueIdOrKey`: string
  - `formId`: string

### `forms.getRequestTypeForm`

Get the portal form attached to a JSM request type (404 if the request type has no form).

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string
  - `requestTypeId`: string

### `forms.listFormTemplates`

List the form templates of a project (portal request forms / intake forms live here).

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `projectIdOrKey`: string

### `forms.listIssueForms`

List the forms attached to an issue/request (agent view).

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `issueIdOrKey`: string

### `jsm.getQueue`

Get a single queue by id.

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string
  - `queueId`: string

### `jsm.getQueueIssues`

Get the issues currently visible in a queue.

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string
  - `queueId`: string
  - `start`: number (default: 0)
  - `limit`: number (default: 50)

### `jsm.getRequestSla`

Get the SLA state (cycles, breach status, remaining time) for a single customer request. Note: SLA *configuration* is not exposed by the public API; only per-request SLA state is.

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `issueIdOrKey`: string — The request/issue id or key, e.g. ISD-42.
  - `start`: number (default: 0)
  - `limit`: number (default: 50)

### `jsm.getRequestType`

Get a request type by id.

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string
  - `requestTypeId`: string

### `jsm.getRequestTypeFields`

Get the field set associated with a request type.

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string
  - `requestTypeId`: string

### `jsm.getRequestTypeGroups`

List the request-type groups of a service desk.

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string

### `jsm.getServiceDesk`

Get a single service desk by id.

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string

### `jsm.listJsmOrganizations`

List organizations available in a service desk (or globally if serviceDeskId omitted).

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string
  - `start`: number (default: 0)
  - `limit`: number (default: 50)

### `jsm.listQueues`

List the queues of a service desk.

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string

### `jsm.listRequestTypes`

List request types in a service desk.

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string
  - `groupId`: string

### `jsm.listServiceDesks`

List all JSM service desks visible to the API-token caller.

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `start`: number (default: 0)
  - `limit`: number (default: 50)

### `jsm.searchKnowledgeBaseArticles`

Search the knowledge-base articles surfaced by a service desk.

- **Group:** `read_jsm_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string
  - `query`: string
  - `highlight`: boolean
  - `start`: number (default: 0)
  - `limit`: number (default: 50)

---

### `write_jsm_admin` (7 tools, 7 operations)

### `forms.createFormTemplate`

Create a form template on a project. `form` must contain a `design` document ({settings, questions, sections, conditions, layout}) — export an existing template with forms.getFormTemplate for the shape. Destructive — requires `commit:true`.

- **Group:** `write_jsm_admin`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `projectIdOrKey`: string
  - `form`: record<string, unknown>
  - `commit`: boolean

### `forms.deleteFormTemplate`

Delete a project form template. **Irreversible** — the full template (design included) is captured in the journal `before` for manual re-creation.

- **Group:** `write_jsm_admin`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `projectIdOrKey`: string
  - `formId`: string
  - `commit`: boolean

### `forms.updateFormTemplate`

Replace a form template in place (rename, edit the design, attach to portal request types via `portalRequestTypeIds`). Captures full before/after.

- **Group:** `write_jsm_admin`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `projectIdOrKey`: string
  - `formId`: string
  - `form`: record<string, unknown>
  - `commit`: boolean

### `jsm.addCustomersToOrganization`

Add customers (by accountId or email) to an organization.

- **Group:** `write_jsm_admin`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `organizationId`: string
  - `usernames`: array<string>
  - `accountIds`: array<string>
  - `commit`: boolean

### `jsm.createRequestType`

Create a request type. Destructive — requires `commit:true`.

- **Group:** `write_jsm_admin`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string
  - `issueTypeId`: string
  - `name`: string
  - `description`: string
  - `helpText`: string
  - `commit`: boolean

### `jsm.deleteRequestType`

Delete a request type. Irreversible.

- **Group:** `write_jsm_admin`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `serviceDeskId`: string
  - `requestTypeId`: string
  - `commit`: boolean

### `jsm.removeCustomersFromOrganization`

Remove customers (by accountId or email) from an organization.

- **Group:** `write_jsm_admin`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `organizationId`: string
  - `usernames`: array<string>
  - `accountIds`: array<string>
  - `commit`: boolean

---

### `read_assets` (3 tools, 10 operations)

### `assets.aqlSearch`

AQL (Assets Query Language) search.

- **Group:** `read_assets`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `qlQuery`: string
  - `page`: number (default: 1)
  - `resultPerPage`: number (default: 25)
  - `includeAttributes`: boolean

### `assets.readObject`

Read a single Assets object: its data (getObject), its references (getObjectReferences), or its change history (getObjectHistory). Use assets.aqlSearch to query objects.

- **Group:** `read_assets`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 3 (select with the required `op` field)

#### op: `getObject`

Get an Assets object by id.

- **Destructive:** no
- **Replaces:** `assets.getObject`
- **Input:**
  - `objectId`: string — Assets object id

#### op: `getObjectReferences`

List references for an Assets object. (Outbound references are VALUES of reference-typed attributes; change them via assets.manageObject updateObject.)

- **Destructive:** no
- **Replaces:** `assets.getObjectReferences`
- **Input:**
  - `objectId`: string — Assets object id

#### op: `getObjectHistory`

Get the change history for an Assets object.

- **Destructive:** no
- **Replaces:** `assets.getObjectHistory`
- **Input:**
  - `objectId`: string — Assets object id

### `assets.readSchema`

Browse Assets schema structure: list object schemas (listObjectSchemas), get one (getObjectSchema), list a schema's object types (listObjectTypes), get one type (getObjectType), list a type's attributes (getObjectTypeAttributes), or export a full schema definition as JSON (exportAssetSchema).

- **Group:** `read_assets`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 6 (select with the required `op` field)

#### op: `listObjectSchemas`

List Assets object schemas in this workspace.

- **Destructive:** no
- **Replaces:** `assets.listObjectSchemas`
- **Input:** _(no parameters beyond `op`)_

#### op: `getObjectSchema`

Get a single object schema by id.

- **Destructive:** no
- **Replaces:** `assets.getObjectSchema`
- **Input:**
  - `schemaId`: string — Object schema id

#### op: `listObjectTypes`

List object types within a schema (schemaId is the PARENT here).

- **Destructive:** no
- **Replaces:** `assets.listObjectTypes`
- **Input:**
  - `schemaId`: string — Object schema id

#### op: `getObjectType`

Get an object type by id.

- **Destructive:** no
- **Replaces:** `assets.getObjectType`
- **Input:**
  - `objectTypeId`: string — Object type id

#### op: `getObjectTypeAttributes`

List attributes of an object type (objectTypeId is the PARENT here).

- **Destructive:** no
- **Replaces:** `assets.getObjectTypeAttributes`
- **Input:**
  - `objectTypeId`: string — Object type id

#### op: `exportAssetSchema`

Export a schema's full definition as JSON. Useful for backup before destructive ops.

- **Destructive:** no
- **Replaces:** `assets.exportAssetSchema`
- **Input:**
  - `schemaId`: string — Object schema id

---

### `write_assets` (4 tools, 13 operations)

### `assets.delete`

Destructive Assets deletions: an object schema — cascades to ALL its types and objects, export first (deleteObjectSchema); an object type and its objects (deleteObjectType); an object type attribute (deleteObjectTypeAttribute); or a single object (deleteObject). All irreversible.

- **Group:** `write_assets`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 4 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `deleteObjectSchema`

Delete an object schema and everything in it (types, objects). Irreversible and heavy — the whole schema definition is captured in the journal `before`. Export it first with assets.readSchema exportAssetSchema.

- **Destructive:** yes
- **Replaces:** `assets.deleteObjectSchema`
- **Input:**
  - `schemaId`: string — Object schema id

#### op: `deleteObjectType`

Delete an object type (and its objects). Irreversible — the type definition is captured in the journal.

- **Destructive:** yes
- **Replaces:** `assets.deleteObjectType`
- **Input:**
  - `objectTypeId`: string — Object type id

#### op: `deleteObjectTypeAttribute`

Delete an attribute from an object type by attribute id. Irreversible. (There is no single-attribute GET, so the journal captures the object type's full attribute list as `before` for reconstruction.)

- **Destructive:** yes
- **Replaces:** `assets.deleteObjectTypeAttribute`
- **Input:**
  - `objectTypeId`: string — Object type id
  - `attributeId`: string — Object type attribute id

#### op: `deleteObject`

Delete an Assets object. Irreversible.

- **Destructive:** yes
- **Replaces:** `assets.deleteObject`
- **Input:**
  - `objectId`: string — Assets object id

### `assets.manageObject`

Create an Assets object with attribute values (createObject) or update an object's attribute values (updateObject — revertible).

- **Group:** `write_assets`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 2 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `createObject`

Create an Assets object (objectTypeId is the PARENT type).

- **Destructive:** yes
- **Replaces:** `assets.createObject`
- **Input:**
  - `objectTypeId`: string — Object type id
  - `attributes`: array<record<string, unknown>> — Attribute VALUE entries: [{objectTypeAttributeId, objectAttributeValues:[{value}]}]
  - `hasAvatar`: boolean

#### op: `updateObject`

Update an Assets object's attribute values. Revertible (restores the prior values of exactly the touched attributes).

- **Destructive:** yes
- **Replaces:** `assets.updateObject`
- **Input:**
  - `objectId`: string — Assets object id
  - `attributes`: array<record<string, unknown>> — Attribute VALUE entries: [{objectTypeAttributeId, objectAttributeValues:[{value}]}]

### `assets.manageSchema`

Model Assets schemas: create/update an object schema (createObjectSchema, updateObjectSchema), an object type (createObjectType, updateObjectType), or an object type attribute definition (createObjectTypeAttribute, updateObjectTypeAttribute).

- **Group:** `write_assets`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 6 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `createObjectSchema`

Create a new object schema.

- **Destructive:** yes
- **Replaces:** `assets.createObjectSchema`
- **Input:**
  - `name`: string — Name (of the schema or object type, per op)
  - `objectSchemaKey`: string — Schema key (short uppercase identifier)
  - `description`: string — Description

#### op: `updateObjectSchema`

Update an object schema's name/key/description. Revertible.

- **Destructive:** yes
- **Replaces:** `assets.updateObjectSchema`
- **Input:**
  - `schemaId`: string — Object schema id
  - `name`: string — Name (of the schema or object type, per op)
  - `objectSchemaKey`: string — Schema key (short uppercase identifier)
  - `description`: string — Description

#### op: `createObjectType`

Create an object type inside a schema (schemaId is the PARENT; iconId required).

- **Destructive:** yes
- **Replaces:** `assets.createObjectType`
- **Input:**
  - `schemaId`: string — Object schema id
  - `name`: string — Name (of the schema or object type, per op)
  - `description`: string — Description
  - `iconId`: string — Icon id (see GET /icon/global for ids)
  - `inherited`: boolean
  - `parentObjectTypeId`: string

#### op: `updateObjectType`

Update an object type's name/description/icon. Revertible.

- **Destructive:** yes
- **Replaces:** `assets.updateObjectType`
- **Input:**
  - `objectTypeId`: string — Object type id
  - `name`: string — Name (of the schema or object type, per op)
  - `description`: string — Description
  - `iconId`: string — Icon id (see GET /icon/global for ids)

#### op: `createObjectTypeAttribute`

Add an attribute definition to an object type (objectTypeId is the PARENT).

- **Destructive:** yes
- **Replaces:** `assets.createObjectTypeAttribute`
- **Input:**
  - `objectTypeId`: string — Object type id
  - `attribute`: record<string, unknown> — The attribute DEFINITION object (name, type, typeValue, cardinality...)

#### op: `updateObjectTypeAttribute`

Update an attribute definition on an object type. Revertible.

- **Destructive:** yes
- **Replaces:** `assets.updateObjectTypeAttribute`
- **Input:**
  - `objectTypeId`: string — Object type id
  - `attributeId`: string — Object type attribute id
  - `attribute`: record<string, unknown> — The attribute DEFINITION object (name, type, typeValue, cardinality...)

### `assets.startImport`

Trigger a pre-configured Assets import by its import id. The import (source, mapping, schedule) is configured in the Assets UI; the API only starts it — there is no CSV-upload endpoint.

- **Group:** `write_assets`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `importId`: string — The configured import's id (from the Assets import UI).
  - `commit`: boolean

---

### `read_automation` (3 tools, 5 operations)

### `automation.readRule`

Read Jira automation rules: list all rule summaries, cursor-paged (listAutomationRules), or get one rule's full definition by UUID (getAutomationRule).

- **Group:** `read_automation`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 2 (select with the required `op` field)

#### op: `listAutomationRules`

List automation rules (summaries) for this site.

- **Destructive:** no
- **Replaces:** `automation.listAutomationRules`
- **Input:**
  - `cursor`: string
  - `limit`: number (default: 50)

#### op: `getAutomationRule`

Retrieve a single automation rule by its UUID.

- **Destructive:** no
- **Replaces:** `automation.getAutomationRule`
- **Input:**
  - `ruleId`: string — The rule UUID

### `automation.readTemplate`

Read automation templates: search the template catalog (searchAutomationTemplates) or get one template by id (getAutomationTemplate). Templates are the easiest way to author a valid rule.

- **Group:** `read_automation`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 2 (select with the required `op` field)

#### op: `searchAutomationTemplates`

Search the catalog of automation rule templates (paginated). Pass `{}` for all, or filter by categories/home, or continue with cursor.

- **Destructive:** no
- **Replaces:** `automation.searchAutomationTemplates`
- **Input:**
  - `payload`: record<string, unknown> (default: {})

#### op: `getAutomationTemplate`

Get an automation rule template by id (description, categories, required parameters).

- **Destructive:** no
- **Replaces:** `automation.getAutomationTemplate`
- **Input:**
  - `templateId`: string — Template id

### `automation.searchManualRules`

Find manually-triggerable automation rules available for a given object (e.g. an issue).

- **Group:** `read_automation`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `payload`: record<string, unknown> — The rule/manual/search request body.

---

### `write_automation` (3 tools, 6 operations)

### `automation.createRuleFromTemplate`

Create an automation rule from a template. `ruleHome` is the scope ARI, e.g. `ari:cloud:jira:{cloudId}:project/{projectId}`. Some templates require `parameters`; templates that reference an external connection (e.g. Confluence) fail unless that connection exists. The created rule can then be exported with automation.readRule getAutomationRule and adapted. Destructive — requires `commit:true`.

- **Group:** `write_automation`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `templateId`: string
  - `ruleHome`: string — Scope ARI the rule is created under.
  - `parameters`: record<string, unknown>
  - `commit`: boolean

### `automation.delete`

Delete an automation rule by UUID. Disables the rule first (the API rejects deleting an enabled rule). **Irreversible** — the full rule config is captured in the journal `before` for manual re-creation.

- **Group:** `write_automation`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `ruleId`: string — The rule UUID.
  - `commit`: boolean

### `automation.manageRule`

Manage automation rules: create from a full rule definition (createAutomationRule), replace one in place (updateAutomationRule), or flip its state (enableAutomationRule, disableAutomationRule). All revertible.

- **Group:** `write_automation`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 4 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `createAutomationRule`

Create an automation rule. `rule` is the rule object (components: trigger, conditions, actions) — the API wraps it as { rule: <rule> }. Revertible (disable + delete).

- **Destructive:** yes
- **Replaces:** `automation.createAutomationRule`
- **Input:**
  - `rule`: record<string, unknown> — The rule object (trigger, conditions, actions)

#### op: `updateAutomationRule`

Replace a rule in place by UUID. Captures full before/after (rule JSON does not patch cleanly). Revertible.

- **Destructive:** yes
- **Replaces:** `automation.updateAutomationRule`
- **Input:**
  - `ruleId`: string — The rule UUID
  - `rule`: record<string, unknown> — The rule object (trigger, conditions, actions)

#### op: `enableAutomationRule`

Enable an automation rule by UUID. Revertible (disable).

- **Destructive:** yes
- **Replaces:** `automation.enableAutomationRule`
- **Input:**
  - `ruleId`: string — The rule UUID

#### op: `disableAutomationRule`

Disable an automation rule by UUID. Revertible (enable).

- **Destructive:** yes
- **Replaces:** `automation.disableAutomationRule`
- **Input:**
  - `ruleId`: string — The rule UUID

---

### `read_customfields` (1 tools, 3 operations)

### `customfields.read`

Read custom fields: paged list/search (listCustomFields), get one field's metadata optionally with contexts (getCustomField), list a field's contexts (listCustomFieldContexts).

- **Group:** `read_customfields`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 3 (select with the required `op` field)

#### op: `listCustomFields`

List Jira custom fields (paginated); filter by query string, field type, or ids.

- **Destructive:** no
- **Replaces:** `customfields.listCustomFields`
- **Input:**
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)
  - `query`: string
  - `type`: array<string>
  - `id`: array<string>

#### op: `getCustomField`

Get a custom field's metadata (and its contexts when includeContexts is true).

- **Destructive:** no
- **Replaces:** `customfields.getCustomField`
- **Input:**
  - `fieldId`: string — Custom field id (e.g. customfield_10011)
  - `includeContexts`: boolean

#### op: `listCustomFieldContexts`

List the contexts (project scopes) for a custom field. Page size caps at 100.

- **Destructive:** no
- **Replaces:** `customfields.listCustomFieldContexts`
- **Input:**
  - `fieldId`: string — Custom field id (e.g. customfield_10011)
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)

---

### `write_customfields` (2 tools, 5 operations)

### `customfields.delete`

Delete a custom field. **Irreversible.** Re-invoke with `commit:true`.

- **Group:** `write_customfields`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `fieldId`: string
  - `commit`: boolean

### `customfields.manage`

Manage custom fields: create one (createCustomField), update name/description/searcher (updateCustomField), assign a field context to projects (assignCustomFieldToProjects), or create/update select-list options (setCustomFieldOptions).

- **Group:** `write_customfields`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 4 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `createCustomField`

Create a new custom field. Revertible (the created field is deletable).

- **Destructive:** yes
- **Replaces:** `customfields.createCustomField`
- **Input:**
  - `name`: string — Field name
  - `description`: string — Field description
  - `type`: string — Field type key, e.g. "com.atlassian.jira.plugin.system.customfieldtypes:textfield".
  - `searcherKey`: string — Searcher key for the field type

#### op: `updateCustomField`

Update a custom field's name, description, or searcher. Revertible.

- **Destructive:** yes
- **Replaces:** `customfields.updateCustomField`
- **Input:**
  - `fieldId`: string — Custom field id (e.g. customfield_10011)
  - `name`: string — Field name
  - `description`: string — Field description
  - `searcherKey`: string — Searcher key for the field type

#### op: `assignCustomFieldToProjects`

Assign a custom field context to projects. Revertible (removes the same project ids).

- **Destructive:** yes
- **Replaces:** `customfields.assignCustomFieldToProjects`
- **Input:**
  - `fieldId`: string — Custom field id (e.g. customfield_10011)
  - `contextId`: string — Field context id
  - `projectIds`: array<string> — Project ids to assign the context to

#### op: `setCustomFieldOptions`

Create and/or update options on a custom field context (select/multi-select fields). Options WITHOUT an id are created; options WITH an id update that existing option.

- **Destructive:** yes
- **Replaces:** `customfields.setCustomFieldOptions`
- **Input:**
  - `fieldId`: string — Custom field id (e.g. customfield_10011)
  - `contextId`: string — Field context id
  - `options`: array<object> — Options to create (no id) or update (with id)

---

### `read_projects` (1 tools, 3 operations)

### `projects.read`

Read Jira projects: paged admin list/search (listJiraProjects), get one project (getJiraProject), or get a project plus components and roles in one call (getJiraProjectDetails).

- **Group:** `read_projects`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 3 (select with the required `op` field)

#### op: `listJiraProjects`

List Jira projects (admin view, paginated). expand values: description, lead, issueTypes, url, projectKeys, permissions, insight.

- **Destructive:** no
- **Replaces:** `projects.listJiraProjects`
- **Input:**
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)
  - `expand`: array<string> — Expand parameters
  - `query`: string
  - `typeKey`: string
  - `orderBy`: enum("name" | "category" | "issueCount" | "lastIssueUpdatedTime") (default: "name")

#### op: `getJiraProject`

Retrieve a single Jira project by key or numeric id.

- **Destructive:** no
- **Replaces:** `projects.getJiraProject`
- **Input:**
  - `project`: string — Project key or numeric id
  - `expand`: array<string> — Expand parameters

#### op: `getJiraProjectDetails`

Retrieve a Jira project plus its components, roles, and notification scheme assignments.

- **Destructive:** no
- **Replaces:** `projects.getJiraProjectDetails`
- **Input:**
  - `project`: string — Project key or numeric id

---

### `write_projects` (1 tools, 2 operations)

### `projects.manage`

Create a Jira project (createJiraProject) or archive one (archiveJiraProject — revertible via restore). Deletion is a separate opt-in tool (projects.delete).

- **Group:** `write_projects`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 2 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `createJiraProject`

Create a Jira project. Not auto-revertible; archive or delete manually if needed.

- **Destructive:** yes
- **Replaces:** `projects.createJiraProject`
- **Input:**
  - `key`: string — Project key (uppercase letters and digits, must start with a letter).
  - `name`: string — Project name
  - `projectTypeKey`: enum("software" | "service_desk" | "business")
  - `projectTemplateKey`: string
  - `leadAccountId`: string — Atlassian accountId of the project lead
  - `description`: string
  - `assigneeType`: enum("PROJECT_LEAD" | "UNASSIGNED")
  - `url`: string

#### op: `archiveJiraProject`

Archive a Jira project. Revertible via restore.

- **Destructive:** yes
- **Replaces:** `projects.archiveJiraProject`
- **Input:**
  - `project`: string — Project key or numeric id

---

### `read_schemes` (3 tools, 13 operations)

### `schemes.readAccess`

Read access-control schemes: list permission schemes (listPermissionSchemes), get one (getPermissionScheme), list notification schemes (listNotificationSchemes), get one (getNotificationScheme).

- **Group:** `read_schemes`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 4 (select with the required `op` field)

#### op: `listPermissionSchemes`

List Jira permission schemes.

- **Destructive:** no
- **Replaces:** `schemes.listPermissionSchemes`
- **Input:** _(no parameters beyond `op`)_

#### op: `getPermissionScheme`

Get a single permission scheme (full grant list when expand includes 'all').

- **Destructive:** no
- **Replaces:** `schemes.getPermissionScheme`
- **Input:**
  - `schemeId`: string — Scheme id — a PERMISSION scheme id for the permission ops, a NOTIFICATION scheme id for the notification ops
  - `expand`: array<string> — Expand parameters (e.g. 'all' for the full grant list)

#### op: `listNotificationSchemes`

List notification schemes (paginated).

- **Destructive:** no
- **Replaces:** `schemes.listNotificationSchemes`
- **Input:**
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)

#### op: `getNotificationScheme`

Get a notification scheme.

- **Destructive:** no
- **Replaces:** `schemes.getNotificationScheme`
- **Input:**
  - `schemeId`: string — Scheme id — a PERMISSION scheme id for the permission ops, a NOTIFICATION scheme id for the notification ops
  - `expand`: array<string> — Expand parameters (e.g. 'all' for the full grant list)

### `schemes.readConfig`

Read issue-configuration schemes: list/get workflow schemes (listWorkflowSchemes, getWorkflowScheme), issue-type schemes (listIssueTypeSchemes, getIssueTypeScheme), and field configurations (listFieldConfigurations, getFieldConfiguration).

- **Group:** `read_schemes`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 6 (select with the required `op` field)

#### op: `listWorkflowSchemes`

List workflow schemes (paginated).

- **Destructive:** no
- **Replaces:** `schemes.listWorkflowSchemes`
- **Input:**
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)

#### op: `getWorkflowScheme`

Get a workflow scheme.

- **Destructive:** no
- **Replaces:** `schemes.getWorkflowScheme`
- **Input:**
  - `schemeId`: string — Scheme id — a WORKFLOW scheme id for getWorkflowScheme, an ISSUE-TYPE scheme id for getIssueTypeScheme

#### op: `listIssueTypeSchemes`

List issue-type schemes (paginated).

- **Destructive:** no
- **Replaces:** `schemes.listIssueTypeSchemes`
- **Input:**
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)

#### op: `getIssueTypeScheme`

Get an issue-type scheme.

- **Destructive:** no
- **Replaces:** `schemes.getIssueTypeScheme`
- **Input:**
  - `schemeId`: string — Scheme id — a WORKFLOW scheme id for getWorkflowScheme, an ISSUE-TYPE scheme id for getIssueTypeScheme

#### op: `listFieldConfigurations`

List field configurations (paginated).

- **Destructive:** no
- **Replaces:** `schemes.listFieldConfigurations`
- **Input:**
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)

#### op: `getFieldConfiguration`

Get a field configuration by id.

- **Destructive:** no
- **Replaces:** `schemes.getFieldConfiguration`
- **Input:**
  - `configId`: string — Field configuration id

### `schemes.readScreen`

Read screens and screen schemes: list screens (listScreens), get a screen by id (getScreen), list screen schemes (listScreenSchemes).

- **Group:** `read_schemes`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 3 (select with the required `op` field)

#### op: `listScreens`

List screens (paginated).

- **Destructive:** no
- **Replaces:** `schemes.listScreens`
- **Input:**
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)

#### op: `getScreen`

Get a screen by id.

- **Destructive:** no
- **Replaces:** `schemes.getScreen`
- **Input:**
  - `screenId`: string — Screen id

#### op: `listScreenSchemes`

List screen schemes (paginated).

- **Destructive:** no
- **Replaces:** `schemes.listScreenSchemes`
- **Input:**
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)

---

### `write_schemes` (3 tools, 7 operations)

### `schemes.delete`

Destructive scheme deletions: delete a permission scheme (deletePermissionScheme) or a notification scheme (deleteNotificationScheme). Irreversible.

- **Group:** `write_schemes`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 2 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `deletePermissionScheme`

Delete a permission scheme. Irreversible.

- **Destructive:** yes
- **Replaces:** `schemes.deletePermissionScheme`
- **Input:**
  - `schemeId`: string — Scheme id — a PERMISSION scheme id or NOTIFICATION scheme id depending on the op

#### op: `deleteNotificationScheme`

Delete a notification scheme. Irreversible.

- **Destructive:** yes
- **Replaces:** `schemes.deleteNotificationScheme`
- **Input:**
  - `schemeId`: string — Scheme id — a PERMISSION scheme id or NOTIFICATION scheme id depending on the op

### `schemes.manageNotification`

Manage notification schemes: create one — events settable only at create (createNotificationScheme) — or update name/description (updateNotificationScheme). Both revertible.

- **Group:** `write_schemes`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 2 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `createNotificationScheme`

Create a notification scheme (notificationSchemeEvents settable only here, not via update).

- **Destructive:** yes
- **Replaces:** `schemes.createNotificationScheme`
- **Input:**
  - `name`: string — Notification scheme name
  - `description`: string — Notification scheme description
  - `notificationSchemeEvents`: array<record<string, unknown>>

#### op: `updateNotificationScheme`

Update a notification scheme's name/description.

- **Destructive:** yes
- **Replaces:** `schemes.updateNotificationScheme`
- **Input:**
  - `schemeId`: string — Notification scheme id
  - `name`: string — Notification scheme name
  - `description`: string — Notification scheme description

### `schemes.managePermission`

Manage permission schemes: create one (createPermissionScheme), update one — PUT-replace semantics (updatePermissionScheme), or assign one to a project (assignPermissionSchemeToProject). All revertible.

- **Group:** `write_schemes`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 3 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `createPermissionScheme`

Create a permission scheme.

- **Destructive:** yes
- **Replaces:** `schemes.createPermissionScheme`
- **Input:**
  - `name`: string — Permission scheme name
  - `description`: string — Permission scheme description
  - `permissions`: array<record<string, unknown>> — Permission grant objects

#### op: `updatePermissionScheme`

Update a permission scheme (PUT-replace semantics; carries the existing name for partial updates).

- **Destructive:** yes
- **Replaces:** `schemes.updatePermissionScheme`
- **Input:**
  - `schemeId`: string — Permission scheme id (target for update; the scheme to assign for assignPermissionSchemeToProject)
  - `name`: string — Permission scheme name
  - `description`: string — Permission scheme description
  - `permissions`: array<record<string, unknown>> — Permission grant objects

#### op: `assignPermissionSchemeToProject`

Assign a permission scheme to a project (revertible: restores the prior assignment).

- **Destructive:** yes
- **Replaces:** `schemes.assignPermissionSchemeToProject`
- **Input:**
  - `projectKeyOrId`: string — Project to assign the scheme to
  - `schemeId`: string — Permission scheme id (target for update; the scheme to assign for assignPermissionSchemeToProject)

---

### `read_workflows` (1 tools, 6 operations)

### `workflows.read`

Read workflows: list/search (listWorkflows), get one workflow (getWorkflow), its transitions (getWorkflowTransitions), or a transition's conditions (getWorkflowConditions), validators (getWorkflowValidators), or post-functions (getWorkflowPostFunctions).

- **Group:** `read_workflows`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 6 (select with the required `op` field)

#### op: `listWorkflows`

List/search workflows; each result includes its statuses and transitions inline.

- **Destructive:** no
- **Replaces:** `workflows.listWorkflows`
- **Input:**
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)
  - `queryString`: string
  - `expand`: enum("usage" | "values.transitions")

#### op: `getWorkflow`

Get a single workflow (with statuses + transitions) by name or entity id.

- **Destructive:** no
- **Replaces:** `workflows.getWorkflow`
- **Input:**
  - `workflowId`: string — Workflow name or entity id

#### op: `getWorkflowTransitions`

List the transitions of a workflow (each with its conditions, validators and post-functions).

- **Destructive:** no
- **Replaces:** `workflows.getWorkflowTransitions`
- **Input:**
  - `workflowId`: string — Workflow name or entity id

#### op: `getWorkflowConditions`

Get the conditions for a workflow transition.

- **Destructive:** no
- **Replaces:** `workflows.getWorkflowConditions`
- **Input:**
  - `workflowId`: string — Workflow name or entity id
  - `transitionId`: string — Transition id within the workflow

#### op: `getWorkflowValidators`

Get the validators for a workflow transition.

- **Destructive:** no
- **Replaces:** `workflows.getWorkflowValidators`
- **Input:**
  - `workflowId`: string — Workflow name or entity id
  - `transitionId`: string — Transition id within the workflow

#### op: `getWorkflowPostFunctions`

Get the post-functions for a workflow transition (returned under the API's `actions` key).

- **Destructive:** no
- **Replaces:** `workflows.getWorkflowPostFunctions`
- **Input:**
  - `workflowId`: string — Workflow name or entity id
  - `transitionId`: string — Transition id within the workflow

---

### `write_workflows` (3 tools, 5 operations)

### `workflows.delete`

Delete a workflow by name or entity id. Irreversible. The workflow must not be in use by any scheme.

- **Group:** `write_workflows`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `workflowId`: string — Workflow name or entity id.
  - `commit`: boolean

### `workflows.manage`

Manage workflows: create workflows via the bulk API (createWorkflow), bulk-update them — the only way to change transitions/conditions/validators/post-functions (updateWorkflow), or publish a workflow scheme's draft (publishWorkflowSchemeDraft, async).

- **Group:** `write_workflows`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 3 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `createWorkflow`

Create one or more workflows via the bulk create API. `payload` is the POST /workflows/create body: { scope: { type: 'GLOBAL' | 'PROJECT', project? }, statuses: [{ statusReference, name, statusCategory }], workflows: [{ name, description?, statuses: [...], transitions: [...] }] }. Use workflows.validateCreateWorkflow first if unsure. Revertible.

- **Destructive:** yes
- **Replaces:** `workflows.createWorkflow`
- **Input:**
  - `payload`: record<string, unknown> — Bulk API body; exact required shape depends on the op — see the op descriptions

#### op: `updateWorkflow`

Update one or more workflows via the bulk update API. `payload` is the POST /workflows/update body: { statuses: [...], workflows: [{ id, statuses, transitions, ... }] }. This is how transition, condition, validator and post-function changes are applied — Jira Cloud has no per-transition REST endpoint.

- **Destructive:** yes
- **Replaces:** `workflows.updateWorkflow`
- **Input:**
  - `payload`: record<string, unknown> — Bulk API body; exact required shape depends on the op — see the op descriptions

#### op: `publishWorkflowSchemeDraft`

Publish a workflow SCHEME's draft (this is how workflow changes go live in Jira Cloud — there is no per-workflow publish). Asynchronous: Jira runs the publish as a background task, which this op polls for up to ~60s. A result with status COMPLETE/FAILED/CANCELLED/DEAD is final; if the budget runs out the result is status RUNNING plus the `taskId`, and the caller MUST verify completion itself (GET /rest/api/3/task/{taskId}) — the publish is NOT done yet. `statusMappings` maps old→new statuses for in-flight issues.

- **Destructive:** yes
- **Replaces:** `workflows.publishWorkflowSchemeDraft`
- **Input:**
  - `schemeId`: string — Workflow scheme id (see schemes.readConfig listWorkflowSchemes).
  - `statusMappings`: array<record<string, unknown>>

### `workflows.validateCreateWorkflow`

Dry-run validation for a create-workflow payload (no changes). Body shape matches workflows.manage createWorkflow payload.

- **Group:** `write_workflows`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Input:**
  - `payload`: record<string, unknown>

---

### `read_confluence_admin` (2 tools, 6 operations)

### `confluence.readContent`

Read Confluence content configuration: list page templates (listTemplates), list blueprints (listBlueprints), get a content item's read/update restrictions (getContentRestrictions).

- **Group:** `read_confluence_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 3 (select with the required `op` field)

#### op: `listTemplates`

List Confluence templates available globally or in a specific space (by space KEY).

- **Destructive:** no
- **Replaces:** `confluence.listTemplates`
- **Input:**
  - `spaceKey`: string — Space KEY (v1 vocabulary)
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 25)

#### op: `listBlueprints`

List Confluence blueprints (built-in page templates), optionally scoped to a space KEY.

- **Destructive:** no
- **Replaces:** `confluence.listBlueprints`
- **Input:**
  - `spaceKey`: string — Space KEY (v1 vocabulary)

#### op: `getContentRestrictions`

Get the read/update restrictions on a piece of content.

- **Destructive:** no
- **Replaces:** `confluence.getContentRestrictions`
- **Input:**
  - `contentId`: string — Content id

### `confluence.readSpace`

Read Confluence spaces: list spaces (listConfluenceSpaces), get one space by v2 spaceId (getConfluenceSpace), list a space's permissions (listSpacePermissions).

- **Group:** `read_confluence_admin`
- **Auth method:** api_token
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 3 (select with the required `op` field)

#### op: `listConfluenceSpaces`

List Confluence spaces (cursor-paged), filterable by type/status.

- **Destructive:** no
- **Replaces:** `confluence.listConfluenceSpaces`
- **Input:**
  - `cursor`: string
  - `limit`: number (default: 25)
  - `type`: enum("global" | "personal")
  - `status`: enum("current" | "archived")

#### op: `getConfluenceSpace`

Get a single Confluence space by v2 spaceId.

- **Destructive:** no
- **Replaces:** `confluence.getConfluenceSpace`
- **Input:**
  - `spaceId`: string — Confluence v2 numeric space id

#### op: `listSpacePermissions`

List permissions on a Confluence space (by v2 spaceId).

- **Destructive:** no
- **Replaces:** `confluence.listSpacePermissions`
- **Input:**
  - `spaceId`: string — Confluence v2 numeric space id

---

### `write_confluence_admin` (3 tools, 4 operations)

### `confluence.delete`

Delete a Confluence space. **Irreversible.**

- **Group:** `write_confluence_admin`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `spaceKey`: string
  - `commit`: boolean

### `confluence.manageSpace`

Create a Confluence space (createConfluenceSpace) or update its name/description (updateConfluenceSpace). Keyed by spaceKey; both revertible.

- **Group:** `write_confluence_admin`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 2 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `createConfluenceSpace`

Create a Confluence space (spaceKey becomes the v1 API's key).

- **Destructive:** yes
- **Replaces:** `confluence.createConfluenceSpace`
- **Input:**
  - `spaceKey`: string — Space KEY
  - `name`: string — Space name
  - `description`: string — Space description (plain text)

#### op: `updateConfluenceSpace`

Update a Confluence space's name/description. Revertible.

- **Destructive:** yes
- **Replaces:** `confluence.updateConfluenceSpace`
- **Input:**
  - `spaceKey`: string — Space KEY
  - `name`: string — Space name
  - `description`: string — Space description (plain text)

### `confluence.setContentRestrictions`

Replace restrictions on a piece of content. Requires a paid Confluence plan — the Free plan 403s restriction writes (reads work).

- **Group:** `write_confluence_admin`
- **Auth method:** api_token
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `contentId`: string
  - `restrictions`: array<record<string, unknown>>
  - `commit`: boolean

---

### `delete_projects` (1 tools, 1 operations)

### `projects.delete`

Delete a Jira project. Defaults to move-to-trash (recoverable for ~60 days); pass `permanent: true` to hard-delete with no undo. **Re-invoke with `commit: true` after reviewing the dry-run.**

- **Group:** `delete_projects`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `project`: string — Project key or numeric id.
  - `permanent`: boolean (default: false)
  - `commit`: boolean

---

### `read_agile` (1 tools, 6 operations)

### `agile.read`

Read agile data: list boards (listBoards), get a board (getBoard), list a board's sprints (listSprints) or epics (listEpics), get a sprint (getSprint) or an epic (getEpic).

- **Group:** `read_agile`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 6 (select with the required `op` field)

#### op: `listBoards`

List agile boards (Scrum and Kanban), filterable by type/name/project.

- **Destructive:** no
- **Replaces:** `agile.listBoards`
- **Input:**
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)
  - `type`: enum("scrum" | "kanban" | "simple")
  - `name`: string
  - `projectKeyOrId`: string

#### op: `getBoard`

Get a board by id.

- **Destructive:** no
- **Replaces:** `agile.getBoard`
- **Input:**
  - `boardId`: string — Board id

#### op: `listSprints`

List sprints for a board, filterable by state.

- **Destructive:** no
- **Replaces:** `agile.listSprints`
- **Input:**
  - `boardId`: string — Board id
  - `state`: enum("future" | "active" | "closed")
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)

#### op: `getSprint`

Get a sprint by id.

- **Destructive:** no
- **Replaces:** `agile.getSprint`
- **Input:**
  - `sprintId`: string — Sprint id

#### op: `listEpics`

List epics on a board, filterable by done state.

- **Destructive:** no
- **Replaces:** `agile.listEpics`
- **Input:**
  - `boardId`: string — Board id
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)
  - `done`: boolean

#### op: `getEpic`

Get an epic by id or key.

- **Destructive:** no
- **Replaces:** `agile.getEpic`
- **Input:**
  - `epicId`: string — Epic id or key

---

### `write_agile` (1 tools, 2 operations)

### `agile.manageSprint`

Create a sprint on a Scrum board (createSprint) or update a sprint's name, goal, state, or dates (updateSprint — revertible).

- **Group:** `write_agile`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 2 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `createSprint`

Create a sprint on a Scrum board.

- **Destructive:** yes
- **Replaces:** `agile.createSprint`
- **Input:**
  - `boardId`: string — Board id
  - `name`: string — Sprint name
  - `goal`: string — Sprint goal
  - `startDate`: string — Start date (ISO)
  - `endDate`: string — End date (ISO)

#### op: `updateSprint`

Update a sprint's name/goal/state/dates. Journals only the touched fields; revertible.

- **Destructive:** yes
- **Replaces:** `agile.updateSprint`
- **Input:**
  - `sprintId`: string — Sprint id
  - `name`: string — Sprint name
  - `goal`: string — Sprint goal
  - `state`: enum("future" | "active" | "closed")
  - `startDate`: string — Start date (ISO)
  - `endDate`: string — End date (ISO)
  - `completeDate`: string

---

### `read_filters_dashboards` (2 tools, 4 operations)

### `dashboards.read`

Read dashboards: paged list, optionally scoped to favourite/my (listDashboards), or get one dashboard (getDashboard).

- **Group:** `read_filters_dashboards`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 2 (select with the required `op` field)

#### op: `listDashboards`

List dashboards (all, favourite, or my).

- **Destructive:** no
- **Replaces:** `dashboards.listDashboards`
- **Input:**
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)
  - `filter`: enum("favourite" | "my")

#### op: `getDashboard`

Get a dashboard by id.

- **Destructive:** no
- **Replaces:** `dashboards.getDashboard`
- **Input:**
  - `dashboardId`: string — Dashboard id

### `filters.read`

Read saved filters: paged list/search by name (listFilters) or get one filter (getFilter).

- **Group:** `read_filters_dashboards`
- **Auth method:** oauth
- **Destructive:** no
- **Requires cloudId:** yes
- **Operations:** 2 (select with the required `op` field)

#### op: `listFilters`

List Jira filters (paginated), optionally matching a name.

- **Destructive:** no
- **Replaces:** `filters.listFilters`
- **Input:**
  - `startAt`: number (default: 0)
  - `maxResults`: number (default: 50)
  - `filterName`: string

#### op: `getFilter`

Get a filter by id.

- **Destructive:** no
- **Replaces:** `filters.getFilter`
- **Input:**
  - `filterId`: string — Filter id

---

### `write_filters_dashboards` (4 tools, 6 operations)

### `dashboards.delete`

Delete a dashboard. Irreversible.

- **Group:** `write_filters_dashboards`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `dashboardId`: string
  - `commit`: boolean

### `dashboards.manage`

Create a dashboard (createDashboard: name + share/edit permissions) or update one (updateDashboard — revertible; PUT semantics carry existing values for partial updates).

- **Group:** `write_filters_dashboards`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 2 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `createDashboard`

Create a dashboard.

- **Destructive:** yes
- **Replaces:** `dashboards.createDashboard`
- **Input:**
  - `name`: string — Dashboard name
  - `description`: string — Dashboard description
  - `sharePermissions`: array<record<string, unknown>> — Share permission objects
  - `editPermissions`: array<record<string, unknown>> — Edit permission objects

#### op: `updateDashboard`

Update a dashboard. Revertible.

- **Destructive:** yes
- **Replaces:** `dashboards.updateDashboard`
- **Input:**
  - `dashboardId`: string — Dashboard id
  - `name`: string — Dashboard name
  - `description`: string — Dashboard description
  - `sharePermissions`: array<record<string, unknown>> — Share permission objects
  - `editPermissions`: array<record<string, unknown>> — Edit permission objects

### `filters.delete`

Delete a filter. Irreversible.

- **Group:** `write_filters_dashboards`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Input:**
  - `filterId`: string
  - `commit`: boolean

### `filters.manage`

Create a saved filter (createFilter: name + JQL + sharing) or update one (updateFilter — revertible; PUT semantics carry existing name/JQL for partial updates).

- **Group:** `write_filters_dashboards`
- **Auth method:** oauth
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** yes
- **Operations:** 2 (select with the required `op` field)
- **Shared input:** `commit` _(boolean, optional)_ — commit-positive consent for every op

#### op: `createFilter`

Create a filter.

- **Destructive:** yes
- **Replaces:** `filters.createFilter`
- **Input:**
  - `name`: string — Filter name
  - `jql`: string — JQL the filter runs
  - `description`: string — Filter description
  - `favourite`: boolean — Mark as favourite
  - `sharePermissions`: array<record<string, unknown>> — Share permission objects

#### op: `updateFilter`

Update a filter. Journals only the touched fields; revertible.

- **Destructive:** yes
- **Replaces:** `filters.updateFilter`
- **Input:**
  - `filterId`: string — Filter id
  - `name`: string — Filter name
  - `jql`: string — JQL the filter runs
  - `description`: string — Filter description
  - `favourite`: boolean — Mark as favourite
  - `sharePermissions`: array<record<string, unknown>> — Share permission objects

---

### `admin_org` (17 tools, 17 operations)

### `orgAdmin.addUserToGroup`

Add a user to a group.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** no
- **Input:**
  - `accountId`: string
  - `groupId`: string
  - `commit`: boolean

### `orgAdmin.createGroup`

Create an org-level group.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** no
- **Input:**
  - `name`: string
  - `description`: string
  - `commit`: boolean

### `orgAdmin.deactivateUser`

Deactivate a managed account. Reversible via restoreUser.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** no
- **Input:**
  - `accountId`: string
  - `commit`: boolean

### `orgAdmin.deleteGroup`

Delete an org-level group.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** no
- **Input:**
  - `groupId`: string
  - `commit`: boolean

### `orgAdmin.getGroup`

Get a group by id.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** no
- **Requires cloudId:** no
- **Input:**
  - `groupId`: string

### `orgAdmin.getOrgPolicies`

List org policies (data residency, IP allowlists, etc.).

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** no
- **Requires cloudId:** no
- **Input:**
  - `type`: string

### `orgAdmin.getOrgUser`

Get a managed account's profile.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** no
- **Requires cloudId:** no
- **Input:**
  - `accountId`: string

### `orgAdmin.getUserGroups`

List groups for a managed account.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** no
- **Requires cloudId:** no
- **Input:**
  - `accountId`: string

### `orgAdmin.listGroups`

List org-level groups.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** no
- **Requires cloudId:** no
- **Input:**
  - `cursor`: string

### `orgAdmin.listManagedAccounts`

List all managed accounts in the org (paged).

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** no
- **Requires cloudId:** no
- **Input:**
  - `cursor`: string

### `orgAdmin.listOrgUsers`

Paginated list of managed accounts in the org.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** no
- **Requires cloudId:** no
- **Input:**
  - `cursor`: string

### `orgAdmin.listVerifiedDomains`

List verified domains owned by the org.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** no
- **Requires cloudId:** no
- **Input:** _(no parameters)_

### `orgAdmin.provisionUser`

Provision a new user. Requires `commit:true`.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** no
- **Input:**
  - `email`: string
  - `displayName`: string
  - `profile`: record<string, unknown>
  - `commit`: boolean

### `orgAdmin.queryAuditLog`

Query org audit log with optional filters.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** no
- **Requires cloudId:** no
- **Input:**
  - `from`: string
  - `to`: string
  - `actor`: string
  - `action`: string
  - `product`: string
  - `cursor`: string
  - `limit`: number (default: 100)

### `orgAdmin.removeUserFromGroup`

Remove a user from a group.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** no
- **Input:**
  - `accountId`: string
  - `groupId`: string
  - `commit`: boolean

### `orgAdmin.restoreUser`

Restore a previously deactivated managed account.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** no
- **Input:**
  - `accountId`: string
  - `commit`: boolean

### `orgAdmin.setOrgPolicy`

Set or replace an org policy.

- **Group:** `admin_org`
- **Auth method:** org_admin
- **Destructive:** yes (commit-positive consent enforced)
- **Requires cloudId:** no
- **Input:**
  - `policyId`: string
  - `body`: record<string, unknown>
  - `commit`: boolean

---
